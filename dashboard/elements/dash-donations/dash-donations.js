/**
 * @customElement
 * @polymer
 */
class DashDonations extends Polymer.Element {
	static get is() {
		return 'dash-donations';
	}

	static get properties() {
		return {};
	}
}

customElements.define(DashDonations.is, DashDonations);
